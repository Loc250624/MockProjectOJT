You are a Senior Fullstack Developer working on the existing E-Learning project.

Read every file in this package and inspect the screenshots in references/.

Your task is to implement ONLY these fixes in the existing repository:

1) Admin / All Users:
- Make Reset Password, Change Role, Block/Unblock Account, and Soft-delete Account functional.
- Reuse existing backend/security/password-reset mechanisms whenever present.
- Reset password must respect auth provider: LOCAL can use reset flow; Google/GitHub password is provider-managed.
- Change role must persist and include self/last-admin safety.
- Block must actually prevent account access and be reversible.
- Soft-delete must preserve historical/audit/payment/enrollment data and prevent login.
- Use custom project UI/modal/toast and valid CSRF. Do not disable CSRF.

2) Admin / Categories:
- Fix Edit.
- Fix Delete safely.
- Category metadata can be edited even when it is assigned to courses.
- Unused category can be deleted per existing project pattern.
- A used category must never be blindly hard-deleted.
- Return a proper dependency conflict and show a clear UI.
- If the existing architecture can support it without broad unrelated changes, provide a safe transactional “Reassign courses & delete” flow using a replacement category.

3) Admin / Transactions:
- This project receives VNPAY payments in Vietnamese Dong.
- Remove incorrect USD presentation from Admin Transactions.
- Format all transaction amounts and successful total as VND, preferably vi-VN currency formatting with 0 fraction digits.
- Inspect VNPay ×100 semantics before touching stored amounts. Do not create double conversion.
- Apply VND to detail/export within this feature if present.

Important:
- First inventory the exact files/classes/routes involved.
- Prefer minimal changes.
- Do not refactor or redesign unrelated modules.
- Do not globally replace USD across the entire repository.
- Do not modify unrelated payment state logic unless required for the currency fix.
- Add/update focused tests for service/controller and any relevant frontend logic.
- Run the project’s existing build/test command.
- At the end, report:
  a) files changed,
  b) business rules implemented,
  c) tests run/results,
  d) any migration added and why,
  e) any remaining limitation.
