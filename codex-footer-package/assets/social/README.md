# Social SVG assets

Các SVG dùng `fill="currentColor"` để nhận màu từ CSS.

Chỉ dùng chúng khi dự án chưa có icon package phù hợp. Không cần cài dependency mới.

Khi không có URL thật, icon phải ở trạng thái non-interactive và có tooltip “Đang cập nhật”.
